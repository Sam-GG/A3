package com.example.a3;

import javafx.scene.paint.Color;

public class XSquare extends XShape{
    public XSquare(Color color, int size, int x_coord, int y_coord) {
        super(color, size, x_coord, y_coord);
    }
}
